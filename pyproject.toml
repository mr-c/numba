[build-system]
requires = ["setuptools >= 77.0.3", "versioneer[toml] == 0.28"]
build-backend = "setuptools.build_meta"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "numba/_version.py"
versionfile_build = "numba/_version.py"
tag_prefix = ""
parentdir_prefix = "numba-"
